<nav class="navbar navbar-custom navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button"
            class="navbar-toggle collapsed"
            data-toggle="collapse"
            data-target="#navcollapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            </button>
            
            <a href="{{ path('homepage') }}" class="navbar-brand"><span class="glyphicon glyphicon-pencil"></span> Notes.com</a>
        </div>
        <div class="collapse navbar-collapse" id="navcollapse">
            <ul class="nav navbar-nav">
                <li>
                    <a href="/list">My notes
                        {% if app.session.get('numOfNotes') %}
                            <span class="badge">{{ app.session.get('numOfNotes') }}</span>
                        {% endif %}
                    </a>
                </li>
                <li><a href="#about">About</a></li>
                <li><a href="#contact">Contact Us</a></li>
            </ul>
            <div class="navbar-right">
                <div>
                    {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                        {{ 'layout.logged_in_as'|trans({'%username%': app.user.username|escape|capitalize}, 'FOSUserBundle') }}
                        <div class="btn">
                            <a href="{{ path('fos_user_security_logout') }}" class="btn btn-primary">
                                {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                            </a>
                        </div>
                    {% else %}
                        <div class="btn">
                            <a href="{{ path('fos_user_security_login') }}" class="btn btn-success"><span class="glyphicon glyphicon-user"></span> {{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                        </div>
                        <div class="btn">
                            <a href="{{ path('fos_user_registration_register') }}" class="btn btn-primary">{{ 'layout.register'|trans({}, 'FOSUserBundle') }}</a>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>
</nav>